-- =====================================================================
--  V9__set_admin_by_email.sql
--  관리자 계정을 member_id(가입순서)가 아닌 "이메일" 기준으로 확정
--
--  배경: V8은 member_id IN (1,2)를 ADMIN으로 지정했으나, member_id는
--        가입 순서라 환경마다 누구인지 달라진다(테스트 계정이 1,2번일 수도).
--        실제 운영자 계정은 아래 두 이메일이므로 이메일 기준으로 교정한다.
--
--  주의: 이 마이그레이션은 "실행 시점에 가입돼 있는" 회원만 반영한다.
--        아래 이메일이 아직 가입 전이면 0행 갱신(무해)이며, 가입 후
--        재실행/수동 UPDATE가 필요하다. (현재는 둘 다 가입 완료 상태)
-- =====================================================================

-- 1. 전부 USER로 초기화 (V8에서 1,2번에 잘못 부여된 ADMIN 해제)
UPDATE member SET role = 'USER';

-- 2. 실제 운영자 계정만 ADMIN
UPDATE member SET role = 'ADMIN'
 WHERE email IN ('danna0326@naver.com', 'gaza1268@naver.com');