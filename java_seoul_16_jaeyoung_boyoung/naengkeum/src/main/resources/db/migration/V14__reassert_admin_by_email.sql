-- =====================================================================
--  V14__reassert_admin_by_email.sql
--  운영자 계정 관리자(ADMIN) 권한 재적용
--
--  배경: V9는 "실행 시점에 가입돼 있는 회원만" 반영한다(해당 파일 주석 참고).
--        운영 DB는 첫 배포 때 member 테이블이 비어 있어 V9의 UPDATE가 0행
--        갱신됐고, 운영자 계정은 그 이후에 가입해 role='USER'로 남았다.
--        Flyway는 V9를 재실행하지 않으므로, 가입 완료된 지금 새 버전으로
--        다시 ADMIN을 부여한다.
--
--  주의: 적용 후 해당 사용자는 JWT를 새로 받아야 권한이 반영된다
--        (로그아웃 → 재로그인). 기존 토큰의 role claim은 USER 그대로이기 때문.
-- =====================================================================

UPDATE member SET role = 'ADMIN'
 WHERE email IN ('danna0326@naver.com', 'gaza1268@naver.com');
