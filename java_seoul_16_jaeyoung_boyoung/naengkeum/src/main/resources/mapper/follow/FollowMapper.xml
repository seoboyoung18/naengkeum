<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
  FollowMapper XML — Follow 그룹 (follow 테이블, API 명세 §7).
  - 팔로우 대상은 활성 회원만(소프트 삭제된 회원 제외).
  - 등록은 uq_follow로 중복 차단되지만 서비스에서 사전 체크 후 409.
-->
<mapper namespace="com.fridgefamer.mapper.follow.FollowMapper">

    <select id="memberExists" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM member
             WHERE member_id = #{memberId} AND is_active = 1
        )
    </select>

    <select id="exists" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM follow
             WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
        )
    </select>

    <insert id="insertFollow">
        INSERT INTO follow (follower_id, followee_id)
        VALUES (#{followerId}, #{followeeId})
    </insert>

    <delete id="deleteFollow">
        DELETE FROM follow
         WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM follow WHERE followee_id = #{memberId}
    </select>

</mapper>